# MQTT broker (required)
MQTT_HOST=your_mqtt_broker_ip
MQTT_PORT=1883
MQTT_USERNAME=your_mqtt_user
MQTT_PASSWORD=your_mqtt_password

# Log capture (optional, defaults shown)
LOG_DIR=/data/logs
LOG_MAX_SIZE_MB=10
LOG_BACKUP_COUNT=3

# Timezone (optional, defaults to UTC)
TZ=America/Los_Angeles
